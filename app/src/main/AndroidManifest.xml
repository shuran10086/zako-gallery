<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zako.gallery">

    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <!-- Required for the full-screen overlay: without this declaration the app
         does not appear in the system "display over other apps" list at all. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Photo access: requested on first launch so the toggle exists in system
         settings, and checked before the app reads shared storage for the
         album browser (adding images) or accepts shared images. Reading
         public files needs it; the system photo picker is not used. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/icon"
        android:roundIcon="@drawable/icon"
        android:theme="@style/Theme.RandomWallpaper">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PermissionGuideActivity"
            android:exported="false" />

        <activity
            android:name=".GalleryListActivity"
            android:exported="false" />

        <activity
            android:name=".GalleryDetailActivity"
            android:exported="false" />

        <activity
            android:name=".AlbumBrowserActivity"
            android:exported="false" />

        <!-- Accepts images shared from other apps (gallery, WeChat, ...) so
             pictures the app cannot list itself can still be imported. -->
        <activity
            android:name=".ShareImportActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <service
            android:name=".WallpaperChangeService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".PopupOverlayService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- BOOT_COMPLETED and MY_PACKAGE_REPLACED are protected broadcasts only
             the system can send, so exported=true leaks nothing. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
